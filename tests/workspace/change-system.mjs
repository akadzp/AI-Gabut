import assert from "node:assert/strict"; import fs from "node:fs/promises"; import os from "node:os"; import path from "node:path"; const root = await fs.mkdtemp( path.join(os.tmpdir(), "ai-gabut-workspace-") ); const storageRoot = path.join(root, "storage"); process.env.AI_WORKSPACE_ROOT = root; try { await fs.writeFile( path.join(root, "sample.js"), "export const value = 1;\n", "utf8" ); const { createStorage } = await import( "../../core/storage/index.js" ); const { createStateService } = await import( "../../core/storage/state/index.js" ); const { createWorkspaceChangeSystem } = await import( "../../core/workspace/change-system.js" ); const storage = createStorage({ root: storageRoot }); const state = createStateService({ storage, namespace: "test-workspace", scopes: ["changes"] }); const changes = createWorkspaceChangeSystem({ stateService: state }); const planned = await changes.create({ task: "update sample.js", paths: ["sample.js"] }); assert.equal( planned.status, "planned" ); assert.equal( typeof planned.id, "string" ); const captured = await changes.capture( planned.id, "sample.js" ); assert.match( captured.baselineHash, /^[a-f0-9]{64}$/ ); assert.equal( captured.path, "sample.js" ); const edited = await changes.edit( planned.id, { path: "sample.js", operation: "replace", oldText: "value = 1", newText: "value = 2" } ); assert.equal( edited.ok, true ); assert.match( edited.afterHash, /^[a-f0-9]{64}$/ ); const persisted = await changes.get( planned.id ); assert.equal( persisted.status, "changed" ); assert.equal( persisted.id, planned.id ); assert.equal( persisted.files["sample.js"].lastEdit.afterHash, edited.afterHash ); const reopened = createWorkspaceChangeSystem({ stateService: createStateService({ storage, namespace: "test-workspace", scopes: ["changes"] }) }); const persistedAfterReopen = await reopened.get( planned.id ); assert.equal( persistedAfterReopen.id, planned.id ); await reopened.close( planned.id, "completed" ); const completed = await reopened.get( planned.id ); assert.equal( completed.status, "completed" ); const listed = await reopened.list(); assert.equal( listed.length, 1 ); assert.equal(listed[0], planned.id); console.log( "Workspace change-system contract: PASS" ); } finally { await fs.rm( root, { recursive: true, force: true } ); } 

